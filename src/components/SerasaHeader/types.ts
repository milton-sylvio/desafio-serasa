import {
  BackgroundProps,
  ColorProps,
  FlexboxProps,
  LayoutProps,
  PositionProps,
} from 'styled-system';

export type IPropsStyles = BackgroundProps &
  ColorProps &
  FlexboxProps &
  LayoutProps &
  PositionProps;
